# Sweep of 2026-09-06 — badgairbros.com

Garrett asked for a once-over of both repos. This is the record for the site; Acrefile's is `Acrefile/docs/audit-2026-09-06.md`. Status: **done** the same day, **parked** on purpose, or **open**. Two items were left for Garrett: softening "Book a flight" until Part 107 is in hand, and whether to shrink the empty Listen band.

## 1. Did not make sense

| Finding | Status |
|---|---|
| "First flight, in numbers" band counted up to 35 photos, 9 minutes, 53 seconds to stitch, flown 2 Sep, with "620 photos" a few pixels below it. Every number was hardcoded twice; `site.flight` was never read. | done — one source: `site.flight` drives the band, the pipeline strip, and the scene copy |
| "Minutes" meant three things (38 flying, 10 stitching, 9 on the tile). | done — `flightMinutes` and `stitchMinutes`, named in the copy |
| Slider caption said "H-3 from our first flight", "wet corner, skips, cattle"; the scene above said brown patch, waterway, thin corner. | done — caption says what the slider is: the 2 September test over the northeast corner |
| Acres written 65.3 and 65. | done — 65.3 |
| "Book a flight" and "Fly a field with us this fall" on a site that says Part 107 is in progress. | **Garrett's call** — left as is |
| Listen band is a full dark section holding one "coming soon" box with three entry points. | **Garrett's call** — left as is |
| Hero podcast CTA gated on the name only, the target on name and feed. | done — same gate |
| "Opened from a text" twice within eleven lines. | done |
| Two mailto subjects into one inbox. | done — one subject |
| Pricing wording: no numbers, "ask for a quote". | fine |

## 2. No longer used

| Finding | Status |
|---|---|
| `public/h3-ortho.jpg` (the corner ortho), unreferenced. | done — deleted |
| `public/podcast-cover.png`, an RSS.com upload artifact in `public/`. | done — moved to `docs/brand/` |
| Dead CSS: `.mask`/lineup, `.typed`/typing/caret, `.flight-path`/draw, `.waypoint`/pop, and their reduced-motion selectors; `--moss-bright`; `--font-sans`. | done — removed |
| `Episode.audio` parsed, never rendered; `Forecast.fetchedAt` computed, never rendered. | done — "as of" stamp on the grains page; audio left for a future player |
| `podcast.apple` / `podcast.spotify` null. | parked — need an episode first |

## 3. Needed update

| Finding | Status |
|---|---|
| `CLAUDE.md` said the hero was `h3-ortho.jpg` (2 Sep), the path was illustrative, the podcast was not built, the domain was not attached, the site was static with no fetches. | done — rewritten |
| `README.md` three lines, no dev command. | done |
| `docs/podcast.md` said the Listen band would list four episodes; blockers for Apple/Spotify not stated. | done |
| `next.config.ts` placeholder comment. | done |
| The `<div id="mapping" />` anchor had zero height so the nav never underlined "Mapping". | done — the scene section carries the id |

## 4. Needed heavy work

| Finding | Status |
|---|---|
| No mobile menu: on phones the section links and `/grains` were unreachable; footer omitted Grains. | done — disclosure menu under the mark; footer links |
| `--ink-faint` at 3.45:1 on paper, used on nearly every small label; wheat on moss 3.11:1; paper/60 and /70 on moss under 4.5:1. | done — faint raised; moss-band text lifted |
| Pinned scene: `h-screen` clipped under phone address bars; no `prefers-reduced-motion`; inactive steps read aloud; step text overflowed at 375 px. | done — `h-dvh`, static "filed" state under reduced motion, `aria-hidden`, taller text block |
| Compare slider: satellite side had empty alt, no `aria-valuetext`, Home/End unhandled, arrows scrolled the page, no `touch-action`. | done |
| `.reveal` hides content until JS runs, no `noscript` fallback. | done — `noscript` rule |
| Scroll scene re-renders ~75 elements per frame; 3.4 KB path re-parsed each frame in `offsetPath`. | open — fine on a laptop; revisit if a phone stutters |
| No sitemap, robots, Open Graph image, canonical. | done — `sitemap.ts`, `robots.ts`, `opengraph-image.jpg` from the H-3 ortho, canonical |
| Grains page baked a "try again in a minute" message for an hour when Open-Meteo blipped at build. | done — copy says check back later; forecast carries its own "as of" time |
| `key={ep.title}` on episodes. | done — url or title plus index |
| Feed parsed with regex; fine for four items. | parked |
